export enum Mode {
	dev = '--dev',
	build = '--build'
}

export enum Dirp {
	tmp = 'tmp',
	root = 'root'
}

export enum Tasks {
	views = 'views',
	viewsPretty = 'viewsPretty',
	scripts = 'scripts',
	scriptsMinify = 'scriptsMinify',
	styles = 'styles',
	stylesFull = 'stylesFull',
	cleaner = 'cleaner',
	cleanerFinal = 'cleanerFinal',
	server = 'server',
	assets = 'assets',
}

